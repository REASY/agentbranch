# AgentBranch Smoke Fixture

This fixture is intentionally small and sandbox-oriented.

- `README.md` is the tracked file that smoke phases edit and sync back.
- `agbranch run ... bash -lc ...` proves guest command execution without assuming a preinstalled language toolchain.
